<%@page pageEncoding="UTF-8" import="org.bd.modelo.mostrar_persona,java.util.List, org.bd.modelo.DBConection"%>
<%
DBConection conn = (DBConection)session.getAttribute("conn"); 
List<mostrar_persona> mp = mostrar_persona.GetMostrarPersonas(conn);
%>
<!DOCTYPE html>
<html>
  <head>
    <link href="dashboard.css" rel="stylesheet">
    <title>Dashboard</title>
  </head>
  <body>
    <div id="main-container">
      <table id="main-table">
        <thead>
          <th>Codigo</th>
          <th>Nombre Completo</th>
          <th>ID</th>
        </thead>
        <tbody>
          <%
            for(mostrar_persona p : mp) {
          %>
          <tr>
            <td><%=p.id%></td>
            <td><%=p.nombre%></td>
            <td><%=p.edad%></td>
          </tr>
          <%}%>
        </tbody>
      </table>
    </div>
  </body>
</html>